name: Hourly cron job
on:
  schedule:
    - cron: "0 0 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Hourly cron job
        run: |
          curl --request GET \
          --url 'https://scraper-six.vercel.app/api/otherside-scrape'
