name: Updated collections
on:
  schedule:
    - cron: "* * * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Updated collections
        run: |
          curl --request GET \
          --url 'https://scraper-six.vercel.app/api/otherside-scrape'
